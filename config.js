// Único archivo que hay que tocar si alguna vez se vuelve a implementar
// el backend de Apps Script (la URL cambia cada vez que se crea una
// implementación nueva). Tiene que terminar en /exec.
const API_URL = 'PEGA_ACA_TU_URL_DE_APPS_SCRIPT/exec';
